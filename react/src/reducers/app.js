import {} from 'constants/action-types';

const initialState = {
  output: '0.0'
};

export const app = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
